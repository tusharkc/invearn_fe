import { StyleSheet } from "react-native";

export const addUserDetailsStyle = StyleSheet.create({
  textArea: {
    backgroundColor: "#242424",
    borderRadius: 5,
    borderWidth: 1,
    borderColor: "#434343",
  },
});
